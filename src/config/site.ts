// The canonical production origin — single source of truth for absolute URLs
// (metadataBase, RSS feed links, and anywhere a full URL is required).
export const SITE_URL = "https://novelglide.kai-wu.net";
